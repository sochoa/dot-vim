filetype off                                " Required for Vundle
set nocompatible                            " Be iMproved, required for Vundle
set rtp+=~/.vim/bundle/VundleVim/Vundle.vim " Set the runtime path to include Vundle and initialize
call vundle#begin()
Plugin 'VundleVim/Vundle.vim' " so that it doesn't clean it up when running PluginClean
